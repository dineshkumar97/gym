import styles from "../admin.module.scss";
const Header = () => {
    return (
        <header className={`${styles.site_header}`}>
             header
        </header>
    );
};

export default Header;
